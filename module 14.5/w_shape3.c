#include<stdio.h>
int main(){
    int n;
    scanf("%d", &n);
    int star = 1;
    //for loop for printing the line
    for (int i = 1; i <= n; i++)
    {
        //for loop for printing the star
        for (int j = 1; j <= star;j++)
        {
            
        }
        
    }
    
    return 0;
}